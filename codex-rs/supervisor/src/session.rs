//! 会话自持与持久化（#2 #3 #4；P1-2）。
//!
//! - session↔rollout 映射由 bridge 自持并落盘：threadId 寿命 = mcp-server 进程寿命，
//!   不可作持久句柄（实测超时重连即全灭，rollout 在盘却不可达）。
//! - 会话状态全字段 = 消息历史 + **模型亲和** + 沙箱策略 + cwd + 锁。
//!   实证：`exec resume` 不继承原会话模型（回落 base 缺省）。
//! - 所有权与锁：attach/steering 必须经 bridge 单进程路由，禁止文件级旁路双写
//!   （控制隧道实验：TUI 对外部注入零感知，磁盘一条线、两个进程两条脑）。
